{
  "regions": ["fra1"],
  "buildCommand": "bun run build && node scripts/patch-vercel-output.mjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.ganitel.com/api/:path*"
    }
  ]
}
